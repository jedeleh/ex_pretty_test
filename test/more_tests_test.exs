defmodule MoreTestsTest do
  use ExUnit.Case

  describe "another set of tests" do
    test "first test succeeds" do
      assert true
    end

    test "second test fails" do
      assert false
    end
  end
end
